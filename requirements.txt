# Core pipeline — data plumbing, SQL and the CLI.
# Deliberately excludes the RAG stack (see requirements-rag.txt) so the
# statistical pipeline and CI stay installable in seconds, without torch.
duckdb==1.1.3
pypdf==5.1.0
typer==0.15.1
rich==13.9.4
pytest==8.3.4
# scikit-learn baseline cross-check (pipeline/09_sklearn_baseline.py) --
# lightweight, no torch, so it stays in the core install.
scikit-learn==1.9.0
pandas==3.0.5
numpy==2.4.6
# versioned rules engine (pipeline/rules.py) -- rule definitions are data
# (YAML), not code, so a non-engineer could review them.
pyyaml==6.0.2
# MCP server (pipeline/mcp_server.py) -- pinned below 2.x, which renamed
# FastMCP to MCPServer and changed its API.
mcp==1.29.1
# FastAPI service (pipeline/api.py)
fastapi==0.141.1
uvicorn[standard]==0.52.4
# PostgreSQL persistence (pipeline/db.py)
sqlalchemy==2.0.52
psycopg[binary]==3.3.5
