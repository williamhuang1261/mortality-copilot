apiVersion: v1
kind: Service
metadata:
  name: mortality-copilot-api
  labels:
    app: mortality-copilot-api
spec:
  type: ClusterIP
  selector:
    app: mortality-copilot-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
