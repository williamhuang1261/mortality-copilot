apiVersion: v1
kind: Service
metadata:
  name: mortality-copilot-mcp
  labels:
    app: mortality-copilot-mcp
spec:
  type: ClusterIP
  selector:
    app: mortality-copilot-mcp
  ports:
    - name: mcp
      port: 80
      targetPort: 8001
